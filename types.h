#ifndef TYPES_H
#define TYPES_H

enum axis {
  ROLL,     // Aileron
  PITCH,    // Elevator
  THROTTLE, // Throttle
  YAW,      // Rudder
  AXIS_NUM
};

enum battery {
  BATTERY_TX,
  BATTERY_CH
};

#endif
